/**
 * Java object model and loader for Workflow Description Language (WDL) documents.
 *
 * <p>This package contains the root document model, the WDL 1.x loader, version metadata, and the
 * validator entry points used to apply optional semantic, static-analysis, and linting checks after
 * parsing.
 */
package com.myriad.wdl.model;
